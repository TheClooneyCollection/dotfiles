;;; theme.el --- Theme selection -*- lexical-binding: t; -*-

;; Theme loading happens in early-init so the first frame and bootstrap screen
;; do not flash the default Emacs colors before the theme is available.

(provide 'theme)

;;; theme.el ends here
