# Apply layout to the very first window (when a session is created)
# set-hook -g after-new-session 'split-window -h -c "#{pane_current_path}"; split-window -v -c "#{pane_current_path}"; select-layout main-vertical; select-pane -t 0'

# Apply layout to windows created later
# set-hook -g after-new-window 'split-window -h -c "#{pane_current_path}"; split-window -v -c "#{pane_current_path}"; select-layout main-vertical; select-pane -t 0'

# Automatically rename windows
set-option -g allow-rename on

# Use current pane's directory basename as the window name
set-option -g automatic-rename on
set-option -g automatic-rename-format '#{b:pane_current_path}'

# Default Layout For New Window
# bind c new-window \;\
#   split-window -h -c "#{pane_current_path}" \;\
#   split-window -v -c "#{pane_current_path}" \;\
#   select-pane -t 0

# split panes using v and h
bind '-' split-window -v -c "#{pane_current_path}"
bind '\' split-window -h -c "#{pane_current_path}"
unbind '"'
unbind %

# https://hamvocke.com/blog/a-guide-to-customizing-your-tmux-conf/

# remap prefix from 'C-b' to 'C-a'
# unbind C-b
# set-option -g prefix C-a
# bind-key C-a send-prefix

# switch panes using Alt-arrow without prefix
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# reload config file
# (change file location to your the tmux.conf you want to use)
bind r source-file ~/.tmux.conf

# Enable mouse control (clickable windows, panes, resizable panes)
set -g mouse on

